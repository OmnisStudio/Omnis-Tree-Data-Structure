Do $cinst.$inherited.$construct(pID,pName)
Calculate price as pPrice
Calculate quantity as pQuantity

# TODO: When using in a separate library - creating references to the Node type needs changing to look in separate lib vs this one?